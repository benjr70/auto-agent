# The resolve lane's dry run (bin/auto-agent fire --resolve-dry-run <N>) writes
# its findings file into the fixture Target Project; a real Target Project
# commits research, the fixture never does.
plugin/fixtures/target-project/docs/
__pycache__/

# terraform run by hand in infra/terraform (Setup keeps its data and state outside the repo).
infra/terraform/**/.terraform/
infra/terraform/**/*.tfstate*
